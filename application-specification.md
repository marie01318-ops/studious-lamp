# アプリケーション仕様書（完全版）

## 1. 概要

本アプリケーションは、Google Apps Script（GAS）を基盤として構築する、介護・福祉現場向けの Web アプリケーションである。  
モニタリング予定や会議予定の登録、未完了予定の抽出、支援経過記録の入力、生成 AI（Gemini）による記録文作成支援を一体化し、現場業務の効率化と記録品質の平準化を目的とする。

## 2. 目的

- 利用者ごとのモニタリングや会議予定を Google カレンダーへ即時登録できるようにする
- 未完了の予定を自動で抽出し、対応漏れを防止する
- 健康・生活状態の入力をボタン操作で簡略化し、記録作成時間を短縮する
- Gemini を活用して支援経過記録の文案作成を支援する
- 完了済み予定を視覚的に区別し、進捗管理を容易にする

## 3. 利用対象

- 介護支援専門員
- 相談支援専門員
- 福祉現場の記録担当者
- モニタリング・担当者会議・支援経過入力を行う職員

## 4. システム構成

- フロントエンド: Google Apps Script による Web アプリ画面
- バックエンド: Google Apps Script
- 外部連携:
  - Google カレンダー（メインカレンダー / `primary`）
  - Gemini（記録作成支援用）

## 5. 業務範囲

本アプリケーションが対象とする業務は以下のとおり。

- モニタリング予定の登録
- 担当者会議予定の登録
- 事前メモの入力
- 未完了予定の確認
- 状態選択による記録補助
- 支援経過記録の作成支援
- 完了処理およびカレンダー反映

## 6. 主要機能一覧

### 6.1 カレンダー連携登録

ユーザーが以下の情報を入力し、Google カレンダーのメインカレンダー（`primary`）へ予定を登録する。

- カテゴリー
- 氏名
- 日時
- 事前メモ

登録は即時反映とし、予定作成後ただちにカレンダーへ保存される。

#### 登録タイトル形式

タイトルは、以下のような業務判別しやすい形式とする。

`[カテゴリー] 氏名`

例:

- `モニタリング 山田太郎`
- `担当者会議 佐藤花子`

### 6.2 未完了予定の抽出

当日を基準として、以下の期間内に存在する予定を自動抽出する。

- 開始日: 当日の 2 ヶ月前
- 終了日: 当日の 1 ヶ月後

抽出条件は以下のとおり。

- Google カレンダー `primary` を対象とする
- タイトルに `【完了】` を含まない予定のみを対象とする

抽出結果は一覧表示し、ユーザーが対象の予定を選択して記録入力へ進める構成とする。

### 6.3 状態選択クイック入力

利用者の健康・生活状態を簡単に記録するため、全 13 項目の状態入力をボタン式で提供する。

#### 状態入力項目

- 食事
- 睡眠
- 排泄
- 水分
- 服薬
- 疼痛
- 体調
- 精神状態
- 活動量
- 移動
- 清潔
- 対人関係
- 住環境

#### 各項目の選択肢

各項目に対して、以下のような状態選択を行えるものとする。

- 改善
- 維持
- 悪化
- 変化なし
- 気になる点あり

選択肢の実装詳細は運用に応じて調整可能とするが、少なくとも「改善」「維持」「悪化」を含むこと。

#### UI 要件

- ボタン押下で状態を選択できること
- 選択済みボタンは青色に変化すること
- 選択状態が視覚的に明確であること
- ボタンクリック時に不要な画面スクロールが発生しないこと
- スマートフォンおよび PC の両方で押しやすいボタンサイズであること

### 6.4 AI 記録アシスト

作成したメモや選択した状態をもとに、Gemini へ渡すための記録文素材を整形する。

#### 主な機能

- 作成メモをワンクリックでコピーできること
- Gemini へ貼り付けやすい形式で出力すること
- 入力支援用の見出しボタンを設けること

#### 見出しボタン例

- `（本人）`
- `（家族）`
- `（職員）`
- `（医療）`
- `（生活状況）`

これらの見出しボタンは、記録文の構造化と入力補助を目的とする。

### 6.5 完了処理

記録入力完了時、対象のカレンダー予定に対して以下の更新処理を行う。

- タイトル先頭に `【完了】` を付与する
- 支援経過を予定説明欄へ保存する
- カレンダーの色をグレー（色番号 `8`）へ変更する

#### 完了後タイトル形式

`【完了】[カテゴリー] 氏名`

例:

- `【完了】モニタリング 山田太郎`

## 7. 画面仕様

### 7.1 トップ画面

機能:

- 新規予定登録
- 未完了予定一覧の表示
- 対象予定の選択

表示項目:

- カテゴリー入力欄
- 氏名入力欄
- 日時入力欄
- 事前メモ入力欄
- 登録ボタン
- 未完了予定一覧

### 7.2 記録入力画面

機能:

- 対象者情報の確認
- 状態選択ボタン入力
- 自由記述メモ入力
- 見出しボタン入力補助
- Gemini 連携用コピー
- 完了処理実行

表示項目:

- 予定タイトル
- 予定日時
- 事前メモ
- 13 項目の状態選択エリア
- 自由記述欄
- 見出しボタン群
- コピー用ボタン
- 完了ボタン

## 8. データ項目仕様

### 8.1 予定登録データ

| 項目名 | 内容 | 必須 |
| --- | --- | --- |
| カテゴリー | モニタリング、担当者会議、その他 | 必須 |
| 氏名 | 利用者氏名 | 必須 |
| 日時 | 実施予定日時 | 必須 |
| 事前メモ | 事前確認事項・論点 | 任意 |

### 8.2 記録データ

| 項目名 | 内容 | 必須 |
| --- | --- | --- |
| 対象予定 ID | カレンダーイベント識別子 | 必須 |
| 状態選択結果 | 13 項目の評価結果 | 任意 |
| 自由記述メモ | 面談内容、観察内容など | 任意 |
| AI 連携用整形文 | Gemini に渡す文面 | 任意 |
| 支援経過 | 最終保存する記録本文 | 必須 |

## 9. 処理フロー

### 9.1 新規予定登録フロー

1. ユーザーがカテゴリー、氏名、日時、事前メモを入力する
2. 登録ボタンを押下する
3. GAS が Google カレンダー `primary` に予定を作成する
4. 登録結果を画面へ返却する

### 9.2 未完了予定表示フロー

1. 画面表示時または更新時に GAS がカレンダーを参照する
2. 当日の 2 ヶ月前から 1 ヶ月後までの予定を取得する
3. タイトルに `【完了】` を含む予定を除外する
4. 条件に一致した予定を一覧表示する

### 9.3 記録作成フロー

1. ユーザーが未完了予定を選択する
2. 対象予定の情報を表示する
3. 状態選択ボタンと自由記述で内容を作成する
4. 必要に応じて Gemini 用テキストをコピーする
5. 生成結果または手入力結果を支援経過として整える
6. 完了ボタン押下でカレンダーへ保存する

### 9.4 完了処理フロー

1. 対象イベントのタイトルを `【完了】...` 形式へ更新する
2. 説明欄へ支援経過を保存する
3. イベント色を色番号 `8` に変更する
4. 一覧から当該予定を未完了対象外とする

## 10. 非機能要件

### 10.1 操作性

- ボタン操作中心で直感的に入力できること
- スクロールの意図しない発生を抑制すること
- PC、タブレット、スマートフォンで利用可能であること

### 10.2 性能

- 未完了予定一覧は実用上支障のない速度で表示されること
- カレンダー登録、更新処理はユーザーが待機可能な時間内で完了すること

### 10.3 可用性

- Google アカウント認証下で安定利用できること
- Google カレンダー API または GAS 標準サービスの正常稼働を前提とすること

### 10.4 保守性

- カテゴリーや状態項目の追加変更がしやすい構造であること
- Gemini 連携文面のテンプレート変更が容易であること

## 11. 権限制御

- 利用者は Google アカウントで認証された関係者に限定する
- 操作対象は、アプリ実行者がアクセス権を持つ Google カレンダーに限る
- カレンダーの閲覧・更新権限がない場合はエラーとして扱う

## 12. エラー時の考慮

- 必須項目未入力時は登録不可とし、入力不足を明示する
- カレンダー登録失敗時は失敗理由を表示する
- 完了更新失敗時は支援経過が未保存である可能性を明示する
- Gemini 連携用コピーが失敗した場合は再試行可能とする

## 13. 今後の拡張候補

- 記録テンプレートの職種別切替
- 利用者検索機能
- 過去記録の参照機能
- 完了済み予定の検索機能
- 出力文の定型化、文体調整
- Gemini への直接送信連携

## 14. 補足

本仕様書は初版の完全版ドラフトであり、実装前の詳細設計において以下を別途確定する。

- カテゴリーの正式な選択肢一覧
- 13 項目の正式名称と順序
- 状態選択肢の最終定義
- Gemini に渡すプロンプトテンプレート
- Google カレンダーイベント説明欄の保存フォーマット
