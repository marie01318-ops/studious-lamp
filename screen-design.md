# 画面設計書

## 1. 画面一覧

| 画面 ID | 画面名 | 用途 |
| --- | --- | --- |
| SCR-01 | トップ画面 | 予定登録、未完了予定一覧の表示 |
| SCR-02 | 記録入力画面 | 状態選択、自由記述、AI 連携、完了処理 |
| SCR-03 | 完了メッセージ領域 | 登録完了、更新完了、エラー表示 |

## 2. 画面遷移

1. `SCR-01` 表示
2. 新規登録時はそのまま `SCR-01` 内で登録完了表示
3. 未完了予定一覧から対象を選択
4. `SCR-02` に対象予定を表示
5. 入力完了後、完了処理を実行
6. 成功時は `SCR-03` の完了メッセージを表示し、一覧を更新

## 3. SCR-01 トップ画面

### 3.1 目的

- Google カレンダーへの予定登録
- 未完了予定の確認
- 対象予定の選択

### 3.2 表示エリア

#### A. ヘッダーエリア

- アプリ名
- 業務説明文
- 今日の日付

#### B. 新規予定登録エリア

表示項目:

- カテゴリー選択
- 氏名入力
- 日時入力
- 事前メモ入力
- 登録ボタン
- 入力リセットボタン

入力ルール:

- カテゴリー、氏名、日時は必須
- 事前メモは任意
- 氏名は全角・半角混在を許容
- カテゴリー選択肢は「モニタリング」「担当者会議」「その他」とする

#### C. 未完了予定一覧エリア

表示項目:

- 予定日
- カテゴリー
- 氏名
- タイトル
- 事前メモ概要
- 選択ボタン

表示条件:

- 当日の 2 ヶ月前から 1 ヶ月後まで
- タイトルに `【完了】` を含まない予定のみ

操作:

- 予定選択で `SCR-02` を表示
- 再読込ボタンで一覧再取得

#### D. メッセージエリア

- 正常終了メッセージ
- 入力エラーメッセージ
- API 連携エラーメッセージ

## 4. SCR-02 記録入力画面

### 4.1 目的

- 選択した予定に対する支援経過記録の入力
- AI 連携用テキストの整形
- 完了処理の実行

### 4.2 表示エリア

#### A. 対象予定情報エリア

- タイトル
- カテゴリー
- 氏名
- 予定日時
- 事前メモ

#### B. 状態選択エリア

13 項目をカードまたは行単位で表示し、各項目ごとに状態ボタンを配置する。

対象項目:

- 食事
- 睡眠
- 排泄
- 水分
- 服薬
- 疼痛
- 体調
- 精神状態
- 活動量
- 移動
- 清潔
- 対人関係
- 住環境

状態ボタン:

- 改善
- 維持
- 悪化
- 変化なし
- 気になる点あり

UI 要件:

- 選択済みは青系色で強調
- ボタン押下時に画面がスクロールしない
- 再選択で値を上書き可能

#### C. 自由記述エリア

- 支援経過メモ入力欄
- 所見入力欄
- 次回確認事項入力欄

#### D. 見出し補助エリア

ボタン例:

- `（本人）`
- `（家族）`
- `（職員）`
- `（医療）`
- `（生活状況）`

操作:

- ボタン押下でカーソル位置に見出し文字列を挿入

#### E. AI 連携エリア

- Gemini 連携用整形テキスト表示欄
- コピー用ボタン
- 整形再生成ボタン

#### F. 完了処理エリア

- 完了ボタン
- 戻るボタン

## 5. SCR-03 メッセージ仕様

### 5.1 正常系

- 予定を登録しました
- 支援経過を保存し、予定を完了にしました

### 5.2 異常系

- 必須項目を入力してください
- カレンダー登録に失敗しました
- 予定の更新に失敗しました
- コピーに失敗しました

## 6. レスポンシブ方針

- PC: 左右 2 カラムを基本とする
- タブレット: 上下積みと 2 カラムを併用する
- スマートフォン: 1 カラムで縦積み表示とする

## 7. UI コンポーネント一覧

| コンポーネント ID | 名称 | 用途 |
| --- | --- | --- |
| CMP-01 | SelectField | カテゴリー選択 |
| CMP-02 | TextField | 氏名入力 |
| CMP-03 | DateTimeField | 日時入力 |
| CMP-04 | TextArea | 事前メモ・自由記述 |
| CMP-05 | StatusButtonGroup | 状態選択 |
| CMP-06 | SectionButton | 見出し挿入 |
| CMP-07 | MessageBanner | 通知表示 |
| CMP-08 | EventList | 未完了予定一覧 |
| CMP-09 | ActionButton | 登録、コピー、完了 |
